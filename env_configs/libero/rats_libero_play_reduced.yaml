# RATS + LIBERO play seed — reduced API variant. Same as rats_libero_play.yaml
# but the coding agent gets FrankaLiberoApiReduced (fewer, lower-level helpers).
# With --explore the curiosity proposer invents its own tasks (fresh BDDL + scenes);
# libero_spatial/task_0 below is just the bootstrap.
# Usage:
#   python scripts/run_rats.py --config env_configs/libero/rats_libero_play_reduced.yaml --explore --iterations 50

env:
  _target_: rats.envs.tasks.franka.franka_libero_env.FrankaLiberoCodeEnv
  cfg:
    _target_: rats.envs.tasks.base.CodeExecEnvConfig
    low_level:
      _target_: rats.envs.simulators.libero.FrankaLiberoEnv
      suite_name: libero_spatial
      task_id: 0
      max_steps: 4000
      control_freq: 20
    privileged: false
    apis:
      - FrankaLiberoApiReduced
    prompt: |
      You are controlling a Franka Emika robot with API described below.
      Goal: pick up the black bowl between the plate and the ramekin and place it on the plate
      You may write python code comments for reasoning but ONLY write the executable Python code and do not write it in code fences.
      After this code executes, you will be able to get a new observation of the environment and write new code to complete the task if needed.
      The functions (APIs) below are already imported to the environment. If you want to use numpy, you need to import it explicitly.

api_servers:
  - _target_: rats.serving.launch_pyroki_server.main
    port: 8116
    host: 127.0.0.1
    robot: panda_description
    target_link: panda_hand

  - _target_: rats.serving.launch_contact_graspnet_server.main
    port: 8115
    host: 127.0.0.1

  - _target_: rats.serving.launch_sam3_server.main
    device: cuda
    port: 8114
    host: 127.0.0.1

record_video: true
output_dir: ./outputs/rats_libero_play_reduced
